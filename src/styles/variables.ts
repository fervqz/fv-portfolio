import { SkeletonProps } from "@mui/material";

export default {
    skeleton: {
        variant: 'rectangular',
        animation: 'wave',
    } as SkeletonProps,
    button: {
        borderRadius: '4px',
    },
    card: {
        borderRadius: '4px',
    },
}