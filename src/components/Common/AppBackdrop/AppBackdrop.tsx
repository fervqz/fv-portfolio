import { styled, Backdrop } from "@mui/material";

const AppBackdrop = styled(Backdrop)({
    color: '#fff',
    backgroundColor: 'rgb(0, 0, 0, 0.4)',
    zIndex: 999,
});

export default AppBackdrop;