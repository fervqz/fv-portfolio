import { styled, Button } from "@mui/material";

const AppButton = styled(Button)({
    padding: '15px 30px',
    textTransform: 'none',
    borderRadius: '0px',
    fontWeight: '600',
});

export default AppButton;